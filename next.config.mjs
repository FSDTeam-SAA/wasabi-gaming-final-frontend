/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["res.cloudinary.com","avatar.iran.liara.run"], // Add domains if needed
    },
};

export default nextConfig;
